{
    "id": "bluetooth_device",
    "name": "蓝牙设备检测",
    "semantic": "state",
    "description": "监控蓝牙设备的连接和断开事件",
    "version_code": 1,
    "enabled": false,
    "params": [
        {
            "name": "device_name",
            "label": "设备名关键词",
            "type": "string",
            "default": "",
            "placeholder": "留空匹配任意设备，或输入关键词（如 JBL）"
        },
        {
            "name": "state",
            "label": "触发状态",
            "type": "select",
            "default": "connected",
            "options": ["connected", "disconnected"]
        }
    ]
}
