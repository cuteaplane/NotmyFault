# develop-alpha 相对 master 的更新日志

## 比较范围

- 基线：`master`，`ecfa7c7`，2026-06-12。
- 分支版本：`develop-alpha`，`542d6fc`，2026-09-01。
- 当前版本号：`alpha-0.15.0pre1`。
- 两个分支已经分叉。`master` 有 8 个独有提交，`develop-alpha` 有 170 个独有提交。
- 已跟踪文件的状态差异为 310 个文件、59195 行新增、1707 行删除，其中包含当前工作区里已跟踪文件的修改。
- 未跟踪文件不计入上述行数。“当前工作区”一节单独列出尚未提交的变更。

## Dashboard 与规则编辑

- Dashboard 从单文件页面改为 Vue 3 和 Vite 项目。
- 页面拆分为首页、自动化、插件、运行记录、安全和设置等区域。
- 规则编辑器支持嵌套条件树、执行前检查、动作流水线和动作输出绑定。
- 新增规则草稿、撤销历史、未保存草稿恢复和保存前检查。
- 新增手工试运行、上游数据准备、局部动作测试、停止运行和失败恢复分支。
- 动作可以配置失败后停止、继续或退避重试。
- 运行中心显示步骤时间线、取消状态和经过脱敏的导出数据。
- 插件可以为参数提供编辑按钮和独立扩展页面。Dashboard 不需要认识插件私有数据的内部结构。
- 新增 AI 规则起草。支持流式返回、会话历史、取消、重试、提供商预设和 API 密钥管理。
- 管理员动作和高风险草稿在保存或执行前要求私钥确认。

## 规则与执行引擎

- 配置和规则分别保存到 `config.json` 与 `rules.json`，并继续使用签名、备份和旧格式迁移。
- 规则支持 `event` 单条件和可嵌套的 `condition` 条件树。
- 新增基于 `$ref` 的结构化数据绑定，动作可以读取触发器或前序动作的输出。
- 动作协议新增 `context-v1`，可以读取运行上下文、上一步结果和规则信息。
- 动作取消协议新增 `runtime-v1`，等待中的动作可以响应停止请求。
- 触发器新增 `event-v2`，每个实例拥有独立状态，并按配置指纹匹配事件。
- 新增并发调度器。规则可以限制并发数，并配置重复触发时忽略、排队或重启。
- 新增运行生命周期和运行记录。引擎会记录排队、执行、取消、失败和完成状态。
- 动作参数与返回值按插件清单生成摘要。敏感字段和未声明字段不会写入运行记录。
- 引擎启动、停止、热重载、触发器线程和扩展会话由独立组件管理。
- 新增隔离动作 worker。插件原生代码崩溃时不再直接带走引擎进程。

## 插件 API 与扩展能力

- 新增整数版本的插件 API v1。
- 插件可以通过公开 API 访问平台能力、通用原生调用锁、网络地址检查和私有数据解包。
- 动作清单支持执行协议、取消协议、幂等性、输出、平台、入口文件、运行模式和安全声明。
- 触发器清单支持事件语义、事件协议、输出、平台和所需能力。
- 新增 `contributes` 扩展协议。插件可以贡献命令、参数编辑器、页面、私有数据类型和窗口控制。
- 插件私有值使用带包名、数据类型和版本的 `plugin_data` 信封保存。
- 新增扩展会话、命令调用、页面消息和插件资源读取。
- 用户插件从源码仓库移到平台配置目录下的 `plugins/`。
- 新增 `.nmfp` 插件包的检查、预览、安装、升级、降级、替换回滚和卸载流程。
- 新增插件索引下载与安装接口。
- 插件可以声明安装期构建命令和输出文件。安装前会展示构建风险并要求确认。
- 插件可以按平台声明不同入口。不兼容当前平台的插件会被跳过。
- 插件可以声明所需平台能力。缺少能力时不会执行对应插件。
- 多数内置动作和触发器改用公开插件 API，减少对引擎内部模块的引用。

## 插件与规则安全

- 插件签名改为 Ed25519，并区分内置签名、作者签名和本机确认签名。
- 第三方插件不再接受旧式本地签名。严格模式会在导入插件代码前验证签名和文件清单。
- 完整性清单会检查文件新增、删除和修改，插件物化前还会再次核对全部文件。
- 插件包会拒绝路径穿越、符号链接和目录外文件。
- 安装前使用语法树扫描动态执行、子进程、网络、注册表、文件写入和原生调用。
- 新增借用宿主权限检查。插件不能通过公开 API 获得未声明的管理员、进程或原生能力。
- 管理员代理核对调用模块、函数对象和允许执行的程序名。
- 管理员动作可以要求规则使用私钥确认，并限制必须写成字面量的参数。
- 插件安装和更新使用临时目录与备份。替换失败时恢复旧版本。
- 安全模式分为 strict、normal 和 permissive。构建信息和密钥状态决定启动时采用的模式。

## 平台支持

- Windows 仍是主要开发平台。
- 新增实验性的 Linux 桌面支持，包括启动、托盘、通知、剪贴板、音频设备、输入、窗口、显示器和截图后端。
- Linux API 密钥可以保存到 Secret Service。
- 新增平台能力探测。Dashboard 可以显示能力状态，插件也可以据此决定是否可用。
- Wayland 下无法可靠实现的全局热键和窗口标题检测会明确标记为不可用。
- 显示器、截图、窗口置顶、系统动作和设备触发器改用平台后端。
- Windows 原生调用补充 `argtypes` 与 `restype`，跨线程调用共用原生锁。
- 文本转语音等不稳定的 COM 调用移到子进程。
- 键盘输入支持 UTF-16 代理对，输入录制不会保存密码控件中的键盘事件。

## 本地 API 与桌面后台

- API 后端从单个 `api_server.py` 拆为路由、业务对象、端口和组装代码。
- 路由按引擎、规则、AI、插件、插件交互和设置分组。
- 本地 API 继续监听 `127.0.0.1:19198`，除 `OPTIONS` 外的请求都需要 Bearer token。
- 新增引擎启停、诊断、日志、平台信息和运行记录接口。
- 新增规则检查、管理员确认、保存、手工运行和取消接口。
- 新增插件清单、风险预览、安装、更新、卸载、索引和扩展会话接口。
- 新增 AI 设置、API 密钥、同步草稿、流式草稿和取消接口。
- SSE 事件改由独立事件代理投递。慢订阅者不会阻塞引擎线程。
- `ApplicationPaths` 统一计算配置、规则、日志、运行记录、token、AI 密钥和用户插件路径。
- `SignedConfigStore` 统一处理配置与规则的读取、验签、备份、迁移和原子写入。

## 内置自动化能力

- 当前包含 25 个内置动作和 18 个内置触发器。
- 文件与数据动作包括追加文本、文件操作、剪贴板写入与清空、创建快捷方式和 HTTP 请求。
- 桌面动作包括截图、发送按键、窗口置顶、显示器控制、壁纸、通知和 UIA 自动化。
- 系统动作包括程序与进程控制、音量、媒体、蓝牙、电源计划、锁屏、关机和 PowerShell。
- 触发器覆盖时间与 cron、全局热键、剪贴板、文件夹、进程、窗口、电源、电量、资源、网络、WiFi、USB、音频设备、锁屏和引擎启动。
- 新增文档静默检查，用于确认文件在归档前是否已经停止变化。

## 构建、测试与文档

- `build.py` 统一生成密钥、签名内置插件、签名 `build.json` 和验证完整性。
- 新增 strict、normal 和 permissive 构建模式。
- Dashboard 增加 Vite 构建、JSDOM 挂载测试和插件扩展页面测试。
- Python 测试统一放在 `notmyfault/tests/`，当前包含 54 个 `test_*.py` 文件。
- 新增插件 API、插件扩展、插件打包、规则格式、运行摘要、API 后端和原生调用安全文档。
- 开发依赖、平台依赖和 pytest 选项改由 `requirements-dev.txt`、`requirements.txt` 与 `pytest.ini` 管理。

## 当前工作区

- 插件管理页按内置和用户安装分组，并增加动作/触发器筛选、列表详情切换、权限、版本、包名和可用状态展示。
- Dashboard 在 `pywebviewready` 事件到达后立即刷新引擎状态，不再等待下一轮定时查询。
- 首页、运行记录和设置页删去重复说明，运行筛选区和窄屏插件列表重新排版。
- 五个分散的 UIA 动作合并为一个内置 `uia_automation` 动作。
- `uia_automation` 通过参数选择控件操作、切换窗口、读取文本、等待控件和执行操作宏。
- UIA 选择器、键鼠录制、回放和扩展页面全部归入内置插件目录。
- 宿主不再包含 UIA、键盘、鼠标和录制实现，也不再提供对应的专用插件 API 入口。
- UIA 选择器和操作宏改用插件私有 `plugin_data`。Dashboard 只提供通用编辑入口。
- README 和开发文档中的失效测试路径已经修正。

## 迁移与不兼容变更

- 旧的 `notmyfault.engine`、`notmyfault.api_server`、`notmyfault.app` 和相关单文件模块已经拆到 `core/`、`host/`、`security/` 与 `platform/`。引用内部模块的代码需要改用新路径或公开插件 API。
- 用户插件目录改为平台配置目录下的 `plugins/`。源码仓库中的 `user_plugins/` 不再是运行时安装位置。
- 规则从配置文件中拆到独立的 `rules.json`。启动时会处理旧格式迁移，迁移后应保留配置目录中的签名和备份文件。
- 第三方插件需要满足新的清单字段、权限、签名和文件完整性检查。旧式本地签名不能继续用于第三方插件。
- 使用 `isolated` 的动作不能声明运行时取消协议，管理员动作也不能使用隔离模式。
- 旧 UIA 动作 `uia_control`、`uia_focus_window`、`uia_read_text`、`uia_wait` 和 `uia_macro` 已移除，不会自动迁移。规则需要改为 `uia_automation` 并重新选择控件或录制操作宏。
- `uia_selector` 不再是宿主参数类型。UIA 数据由 `uia_automation` 插件以 `plugin_data` 管理。
- Linux 支持仍处于实验阶段。Wayland、桌面环境、驱动、权限和外部命令会影响插件可用性。
- Dashboard 构建需要 Node.js 18+；从源码运行仍需要 Python 3.11+。

## 当前工作区检查

- `uia_automation` 清单、平台和权限检查通过，未发现借用宿主权限。
- UIA 插件测试 69 项通过。
- 宿主插件边界相关测试 141 项通过，1 项跳过。
- Dashboard 构建、挂载测试和插件扩展页面测试通过。
- API 后端验证命令成功收集 71 项测试。
- `git diff --check` 通过。
